# AgenticAI Environment Variables
# Copy this to .env.local for local development

# Lovable Cloud (Supabase) Configuration
# These are auto-configured in Lovable, no need to set manually
VITE_SUPABASE_URL=your_project_url.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_key_here
VITE_SUPABASE_PROJECT_ID=your_project_id

# Edge Functions use these (auto-configured via Lovable Cloud):
# SUPABASE_URL - Same as VITE_SUPABASE_URL
# SUPABASE_SERVICE_ROLE_KEY - Admin key for edge functions
# LOVABLE_API_KEY - For AI Gateway access (auto-configured)

# Optional: Additional Configuration
# SECRET_KEY=your_encryption_key_for_field_level_encryption
# AUDIT_RETENTION_DAYS=365

# Optional: Vector Database (for future implementation)
# VECTOR_STORE_TYPE=pinecone
# PINECONE_API_KEY=your_pinecone_key
# PINECONE_ENVIRONMENT=your_pinecone_env

# Optional: External Integrations
# GOOGLE_CLIENT_ID=your_google_oauth_client_id
# GOOGLE_CLIENT_SECRET=your_google_oauth_secret
# GOOGLE_CALENDAR_API_KEY=your_calendar_api_key

# Optional: Email Service (Resend)
# RESEND_API_KEY=your_resend_key

# Optional: Monitoring
# SENTRY_DSN=your_sentry_dsn

# Crisis Hotline Configuration (JSON string)
# CRISIS_NUMBERS_JSON='{"US":"988","UK":"116123","CA":"988"}'

# Note: When using Lovable Cloud, most variables are auto-configured.
# Only set custom variables if adding additional integrations.
